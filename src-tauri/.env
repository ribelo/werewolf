# Database configuration for SQLx
DATABASE_URL=sqlite:werewolf.db

# Application environment
RUST_LOG=info

# SQLx compile-time verification settings
# false = connect to database during compilation (development)  
# true = use prepared query cache for compilation (CI/CD after `cargo sqlx prepare`)
# Set to true with offline query cache now generated
SQLX_OFFLINE=true