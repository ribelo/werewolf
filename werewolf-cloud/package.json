{
  "name": "werewolf-cloud",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "@hono/zod-validator": "^0.2.1",
    "@vitest/coverage-v8": "^2.1.4",
    "miniflare": "^3.20240925.0",
    "oxlint": "^1.0.0",
    "pdf-parse": "^1.1.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "wrangler": "4.42.2",
    "zod": "^3.23.8"
  },
  "packageManager": "bun@1.2.20",
  "private": true,
  "scripts": {
    "dev:prep": "bunx wrangler d1 migrations apply werewolf-d1-dev --local --config wrangler.dev.toml",
    "dev": "bun run dev:prep && bunx wrangler dev --local --config wrangler.dev.toml --port 8787 --persist-to .wrangler/state",
    "typecheck": "tsc --build",
    "lint": "bun run lint:oxlint",
    "lint:oxlint": "oxlint --allow all --deny @typescript-eslint/no-explicit-any --tsconfig ./tsconfig.json apps/api/src packages/domain/src scripts",
    "test": "vitest --run --watch=false --config config/vitest.config.ts",
    "check": "bun run typecheck && bun run lint && bun run test",
    "deploy": "./scripts/deploy.sh",
    "deploy:api": "bunx wrangler deploy --config wrangler.worker.toml --env production",
    "deploy:web": "cd apps/web && SVELTE_ADAPTER=cloudflare bun run build && bunx wrangler pages deploy .svelte-kit/cloudflare --project-name werewolf --branch master --commit-dirty=true"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
