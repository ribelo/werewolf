# Example environment variables for local GitHub Actions testing with act
# Copy this to .env.local and modify as needed
# These are loaded when running act

# Cargo environment
CARGO_TERM_COLOR=always
RUST_BACKTRACE=1

# Build configuration
RUSTC_WRAPPER=
RUSTFLAGS=-C target-cpu=native

# SQLx configuration (if using database)
DATABASE_URL=sqlite:werewolf.db

# Tauri build configuration
TAURI_PLATFORM_VERSION=
TAURI_ARCH=

# Local development overrides
CI=true
GITHUB_ACTIONS=true